import type { PageData } from "$lib/data/PageData";

export function load(): PageData {
    return {
        title: "Guides",
        description: "All the finest guides of HCMUT available at your fingertip."
    };
}