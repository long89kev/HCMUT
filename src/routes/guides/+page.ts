import type { PageData } from "$lib/data/PageData";

export function load(): PageData {
    return {
        title: "Guides",
        description: "All the details of HCMUT available at your fingertip."
    };
}